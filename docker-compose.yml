services:
  app:
    build: .
    container_name: yatindoweb_app

  nginx:
    image: nginx:latest
    container_name: laravel_nginx
    ports:
      - "8088:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app